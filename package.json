{
  "name": "mks_billing_softwer",
  "version": "1.0.0",
  "description": "MKS POS & Billing Software",
  "main": "server/server.js",
  "scripts": {
    "build": "npm install --prefix server",
    "start": "node server/server.js",
    "start:server": "npm start --prefix server",
    "start:frontend": "npm start --prefix frontend",
    "install:all": "npm install --prefix server && npm install --prefix frontend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "create-shortcut": "powershell -ExecutionPolicy Bypass -File ./desktop_launcher/tools/create_shortcut.ps1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "MKS IT Solution",
  "license": "ISC",
  "devDependencies": {
    "@angular/cli": "^21.2.24"
  }
}
