"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Campaigns = void 0;
const tslib_1 = require("../../../internal/tslib.js");
const resource_1 = require("../../../core/resource.js");
const PhoneNumbersAPI = tslib_1.__importStar(require("./phone-numbers.js"));
const phone_numbers_1 = require("./phone-numbers.js");
const pagination_1 = require("../../../core/pagination.js");
const headers_1 = require("../../../internal/headers.js");
const path_1 = require("../../../internal/utils/path.js");
class Campaigns extends resource_1.APIResource {
    constructor() {
        super(...arguments);
        this.phoneNumbers = new PhoneNumbersAPI.PhoneNumbers(this._client);
    }
    /**
     * Create a 10DLC campaign under an existing brand. The campaign starts in draft
     * status. Submit it for carrier review using the submit endpoint.
     *
     * @example
     * ```ts
     * const campaign = await client.number10dlc.campaigns.create({
     *   affiliateMarketing: false,
     *   ageGated: false,
     *   brandId: 'brand_abc123',
     *   description:
     *     'Send order status updates and shipping notifications to customers who opted in.',
     *   directLending: false,
     *   embeddedLink: true,
     *   embeddedPhone: false,
     *   name: 'Order Notifications',
     *   numberPooling: false,
     *   sampleMessages: [
     *     'Hi {{name}}, your order #{{order_id}} has shipped! Track it at {{url}}',
     *     'Your order #{{order_id}} has been delivered. Thank you for your purchase!',
     *   ],
     *   subscriberHelp: true,
     *   subscriberOptIn: true,
     *   subscriberOptOut: true,
     *   useCase: 'ACCOUNT_NOTIFICATION',
     * });
     * ```
     */
    create(body, options) {
        return this._client.post('/v1/10dlc/campaigns', { body, ...options });
    }
    /**
     * Get 10DLC campaign
     *
     * @example
     * ```ts
     * const campaign =
     *   await client.number10dlc.campaigns.retrieve('campaignId');
     * ```
     */
    retrieve(campaignID, options) {
        return this._client.get((0, path_1.path) `/v1/10dlc/campaigns/${campaignID}`, options);
    }
    /**
     * Update a 10DLC campaign in draft status. Cannot update after submission.
     *
     * @example
     * ```ts
     * const campaign = await client.number10dlc.campaigns.update(
     *   'campaignId',
     * );
     * ```
     */
    update(campaignID, body, options) {
        return this._client.patch((0, path_1.path) `/v1/10dlc/campaigns/${campaignID}`, { body, ...options });
    }
    /**
     * List 10DLC campaign registrations for this project.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const tenDlcCampaign of client.number10dlc.campaigns.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/v1/10dlc/campaigns', (pagination_1.Cursor), { query, ...options });
    }
    /**
     * Delete 10DLC campaign
     *
     * @example
     * ```ts
     * await client.number10dlc.campaigns.delete('campaignId');
     * ```
     */
    delete(campaignID, options) {
        return this._client.delete((0, path_1.path) `/v1/10dlc/campaigns/${campaignID}`, {
            ...options,
            headers: (0, headers_1.buildHeaders)([{ Accept: '*/*' }, options?.headers]),
        });
    }
    /**
     * Submit a draft campaign for carrier review. The campaign must be in draft status
     * and its brand must be verified. TCR's one-time registration fee is charged from
     * your balance at submission ($15 for standard use cases, $2 for LOW_VOLUME),
     * passed through at cost and refunded if the carrier rejects it. Once approved,
     * the campaign's monthly TCR fee ($10 standard, $2 LOW_VOLUME) is charged from
     * your balance while the campaign is active — see registrationCostCents and
     * monthlyFeeCents on the campaign object.
     *
     * @example
     * ```ts
     * const response = await client.number10dlc.campaigns.submit(
     *   'campaignId',
     * );
     * ```
     */
    submit(campaignID, options) {
        return this._client.post((0, path_1.path) `/v1/10dlc/campaigns/${campaignID}/submit`, options);
    }
    /**
     * Sync the campaign status with the registration provider. Use this to check for
     * approval updates after submission.
     *
     * @example
     * ```ts
     * const response =
     *   await client.number10dlc.campaigns.syncStatus(
     *     'campaignId',
     *   );
     * ```
     */
    syncStatus(campaignID, options) {
        return this._client.post((0, path_1.path) `/v1/10dlc/campaigns/${campaignID}/sync`, options);
    }
}
exports.Campaigns = Campaigns;
Campaigns.PhoneNumbers = phone_numbers_1.PhoneNumbers;
//# sourceMappingURL=campaigns.js.map