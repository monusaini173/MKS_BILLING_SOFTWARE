import { APIResource } from "../core/resource.js";
import { APIPromise } from "../core/api-promise.js";
import { Cursor, type CursorParams, PagePromise } from "../core/pagination.js";
import { RequestOptions } from "../internal/request-options.js";
export declare class PhoneNumbers extends APIResource {
    /**
     * Get details of a specific phone number.
     *
     * @example
     * ```ts
     * const phoneNumber = await client.phoneNumbers.retrieve(
     *   'phoneNumberId',
     * );
     * ```
     */
    retrieve(phoneNumberID: string, options?: RequestOptions): APIPromise<PhoneNumberRetrieveResponse>;
    /**
     * Update a phone number's name or sender assignment.
     *
     * @example
     * ```ts
     * const phoneNumber = await client.phoneNumbers.update(
     *   'phoneNumberId',
     *   { name: 'Support Line' },
     * );
     * ```
     */
    update(phoneNumberID: string, body: PhoneNumberUpdateParams, options?: RequestOptions): APIPromise<PhoneNumberUpdateResponse>;
    /**
     * List all phone numbers owned by this project.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const ownedPhoneNumber of client.phoneNumbers.list()) {
     *   // ...
     * }
     * ```
     */
    list(query?: PhoneNumberListParams | null | undefined, options?: RequestOptions): PagePromise<OwnedPhoneNumbersCursor, OwnedPhoneNumber>;
    /**
     * Purchase an available phone number. Requires a paid plan: the Free plan cannot
     * purchase phone numbers and receives `402` with code `paid_plan_required`. Paid
     * plans include one US number at no charge. The included number is one per account
     * and is granted once: claiming it spends the benefit for good, so releasing that
     * number does not make another one free, and numbers the account already bought do
     * not consume it.
     *
     * @example
     * ```ts
     * const response = await client.phoneNumbers.purchase({
     *   phoneNumber: '+15551234567',
     *   name: 'Primary Line',
     * });
     * ```
     */
    purchase(body: PhoneNumberPurchaseParams, options?: RequestOptions): APIPromise<PhoneNumberPurchaseResponse>;
    /**
     * Release a phone number. The phone number must not be assigned to a sender.
     *
     * @example
     * ```ts
     * await client.phoneNumbers.release('phoneNumberId');
     * ```
     */
    release(phoneNumberID: string, options?: RequestOptions): APIPromise<void>;
    /**
     * Get regulatory requirements for purchasing phone numbers in a specific country.
     * Some countries require additional documentation (addresses, identity documents)
     * before phone numbers can be activated.
     *
     * @example
     * ```ts
     * const response = await client.phoneNumbers.requirements({
     *   countryCode: 'xx',
     * });
     * ```
     */
    requirements(query: PhoneNumberRequirementsParams, options?: RequestOptions): APIPromise<PhoneNumberRequirementsResponse>;
    /**
     * Search for available phone numbers to purchase by country and type.
     *
     * @example
     * ```ts
     * const response = await client.phoneNumbers.searchAvailable({
     *   countryCode: 'xx',
     * });
     * ```
     */
    searchAvailable(query: PhoneNumberSearchAvailableParams, options?: RequestOptions): APIPromise<PhoneNumberSearchAvailableResponse>;
}
export type OwnedPhoneNumbersCursor = Cursor<OwnedPhoneNumber>;
export interface AvailablePhoneNumber {
    capabilities: PhoneNumberCapabilities;
    phoneNumber: string;
    pricing: PhoneNumberPricing;
    friendlyName?: string;
    locality?: string;
    region?: string;
}
export interface OwnedPhoneNumber {
    id: string;
    capabilities: Array<string>;
    createdAt: string;
    phoneNumber: string;
    pricing: OwnedPhoneNumberPricing;
    status: PhoneNumberStatus;
    /**
     * Optional custom name for the phone number.
     */
    name?: string;
    nextRenewalDate?: string;
    /**
     * Sender ID if the phone number is assigned to a sender.
     */
    senderId?: string;
    updatedAt?: string;
}
export interface OwnedPhoneNumberPricing {
    /**
     * Whether this is a free number.
     */
    isFreeNumber?: boolean;
    /**
     * Monthly cost in cents.
     */
    monthlyCost?: number;
    /**
     * Monthly price in USD.
     */
    monthlyPrice?: number;
    /**
     * One-time purchase cost in cents.
     */
    upfrontCost?: number;
}
export interface PhoneNumberCapabilities {
    mms?: boolean;
    sms?: boolean;
    voice?: boolean;
}
export interface PhoneNumberPricing {
    /**
     * Whether this number qualifies as the plan-included US number on paid plans. The
     * benefit is one per account: it is never offered again once claimed, not even
     * after the number is released.
     */
    isFreeEligible?: boolean;
    /**
     * Monthly price in USD.
     */
    monthlyPrice?: number;
    /**
     * One-time purchase price in USD.
     */
    upfrontPrice?: number;
}
export type PhoneNumberStatus = 'active' | 'suspended' | 'pending';
export type PhoneNumberType = 'local' | 'national' | 'tollFree';
/**
 * A group of requirements for a specific country/phone type combination.
 */
export interface Requirement {
    id: string;
    action: string;
    countryCode: string;
    phoneNumberType: string;
    requirementTypes: Array<RequirementType>;
}
/**
 * Acceptance criteria for a requirement.
 */
export interface RequirementAcceptanceCriteria {
    allowedValues?: Array<string> | null;
    maxLength?: number | null;
    minLength?: number | null;
    regexPattern?: string | null;
}
/**
 * Type of requirement field.
 */
export type RequirementFieldType = 'textual' | 'address' | 'document' | 'action';
/**
 * A specific requirement type within a requirement group.
 */
export interface RequirementType {
    id: string;
    description: string;
    name: string;
    /**
     * Type of requirement field.
     */
    type: RequirementFieldType;
    /**
     * Acceptance criteria for a requirement.
     */
    acceptanceCriteria?: RequirementAcceptanceCriteria;
    example?: string | null;
}
export interface PhoneNumberRetrieveResponse {
    phoneNumber: OwnedPhoneNumber;
}
export interface PhoneNumberUpdateResponse {
    phoneNumber: OwnedPhoneNumber;
}
export interface PhoneNumberPurchaseResponse {
    phoneNumber: OwnedPhoneNumber;
}
export interface PhoneNumberRequirementsResponse {
    items: Array<Requirement>;
}
export interface PhoneNumberSearchAvailableResponse {
    items: Array<AvailablePhoneNumber>;
}
export interface PhoneNumberUpdateParams {
    /**
     * Custom name for the phone number. Set to null to clear.
     */
    name?: string | null;
    /**
     * Sender ID to assign the phone number to. Set to null to unassign.
     */
    senderId?: string | null;
}
export interface PhoneNumberListParams extends CursorParams {
    /**
     * Filter by phone number status.
     */
    status?: PhoneNumberStatus;
}
export interface PhoneNumberPurchaseParams {
    /**
     * Phone number in E.164 format.
     */
    phoneNumber: string;
    /**
     * Optional custom name for the phone number.
     */
    name?: string;
}
export interface PhoneNumberRequirementsParams {
    /**
     * Two-letter ISO country code.
     */
    countryCode: string;
    /**
     * Type of phone number (local, mobile, tollFree).
     */
    type?: PhoneNumberType;
}
export interface PhoneNumberSearchAvailableParams {
    /**
     * Two-letter ISO country code.
     */
    countryCode: string;
    /**
     * Comma-separated capabilities the number must have: `sms`, `voice`, `mms`.
     * Numbers missing any of them are dropped.
     */
    capabilities?: string;
    /**
     * Search for numbers containing this string.
     */
    contains?: string;
    /**
     * Maximum number of results to return.
     */
    limit?: number;
    /**
     * Type of phone number to search for.
     */
    type?: PhoneNumberType;
}
export declare namespace PhoneNumbers {
    export { type AvailablePhoneNumber as AvailablePhoneNumber, type OwnedPhoneNumber as OwnedPhoneNumber, type OwnedPhoneNumberPricing as OwnedPhoneNumberPricing, type PhoneNumberCapabilities as PhoneNumberCapabilities, type PhoneNumberPricing as PhoneNumberPricing, type PhoneNumberStatus as PhoneNumberStatus, type PhoneNumberType as PhoneNumberType, type Requirement as Requirement, type RequirementAcceptanceCriteria as RequirementAcceptanceCriteria, type RequirementFieldType as RequirementFieldType, type RequirementType as RequirementType, type PhoneNumberRetrieveResponse as PhoneNumberRetrieveResponse, type PhoneNumberUpdateResponse as PhoneNumberUpdateResponse, type PhoneNumberPurchaseResponse as PhoneNumberPurchaseResponse, type PhoneNumberRequirementsResponse as PhoneNumberRequirementsResponse, type PhoneNumberSearchAvailableResponse as PhoneNumberSearchAvailableResponse, type OwnedPhoneNumbersCursor as OwnedPhoneNumbersCursor, type PhoneNumberUpdateParams as PhoneNumberUpdateParams, type PhoneNumberListParams as PhoneNumberListParams, type PhoneNumberPurchaseParams as PhoneNumberPurchaseParams, type PhoneNumberRequirementsParams as PhoneNumberRequirementsParams, type PhoneNumberSearchAvailableParams as PhoneNumberSearchAvailableParams, };
}
//# sourceMappingURL=phone-numbers.d.ts.map